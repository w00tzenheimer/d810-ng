# ast-grep configuration for d810-ng
# Run: sg scan --config sgconfig.yml

ruleDirs:
  - rules

# Exclude test directories + vendored third-party code from scanning
ignore:
  - tests/
  - tools/scripts/
  - src/d810/_vendor/
