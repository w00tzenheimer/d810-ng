---
id: d81-38ha
status: in_progress
deps: []
links: []
created: 2026-07-15T20:45:37Z
type: feature
priority: 1
assignee: w00tzenheimer
tags: [ui, config-v2, diagnostics]
---
# Function-centric deobfuscation workbench UI

Design the D810 IDA user experience around function-scoped, evidence-driven deobfuscation rather than a flat global rule list. The work includes the config-v2 round-trip correctness prerequisite and a native dockable workbench specification.

## Acceptance Criteria

A committed design spec defines personas, goals and non-goals, config-v2 correctness requirements, workbench information architecture, a registered-pass Recipe Composer, reuse of existing saved function rule overrides, a structured SQLite diagnostic explorer and transactional snapshot/database cleaner, state/data flow, error and abstention semantics, the established pure `*_logic.py` action-testing pattern, thin Qt boundaries, staged delivery, and unit/runtime/e2e/manual IDA verification criteria.


## Notes

**2026-07-15T20:49:29Z**

Drafted docs/superpowers/specs/2026-07-15-deobfuscation-workbench-ui-design.md. Selected compatibility repair plus a function-centric native workbench; implementation is decomposed into five independently planned slices, beginning with config-v2 truthfulness.

**2026-07-15T22:58:47Z**

Approved amendment scope: compose registered passes without Python authoring in IDA; preserve existing function-rule/tag/note saves while adding a sibling full function-recipe record; add a dedicated diagnostic SQLite explorer with deterministic latest sorting and exact selected/bulk snapshot and database cleanup; follow the existing pure `*_logic.py` plus `tests/unit/ui/test_*_logic.py` action-testing convention.

**2026-07-15T23:52:40Z**

Remaining approved scope decomposed into child tickets tcvpu-nyvc (Slice 1), tcvpu-z1b4 (Slice 2), tcvpu-guv6 (Slice 3), tcvpu-004o (Slice 4), tcvpu-qzth (Slice 5), tcvpu-u83u (Diagnostics D0), and tcvpu-oij1 (Diagnostics D1). Terminal-only implementation continues; live IDA/Qt exploration is deferred.

**2026-07-16T00:14:16Z**

Slice 1 read-only workbench is terminal-code complete through 3667b9245: immutable manager snapshots, pass manifests/preflight, outcomes/rule scope/stats/artifacts, pure UI projection/export/action logic, thin native dock adapter, and stable Stats redirect. 5757 unit tests passed. Live Qt acceptance remains deferred; next implementation target is Slice 2 (tcvpu-z1b4).

**2026-07-16T00:27:41Z**

Slice 2 scoped interaction is terminal-code complete through ea212f738: recon-only Analyze, exact-once existing Deobfuscate/FunctionRules reuse, immutable generation-bound commands, pure stale-result rejection, and thin panel dispatch. 5776 unit tests passed. Live command acceptance remains deferred; next target is Slice 3 (tcvpu-guv6).

**2026-07-16T18:27:02Z**

The cross-session GUI worklist now lives at
`docs/superpowers/plans/2026-07-16-d810-gui-session-worklist.md`. It preserves
the implemented-vs-live-accepted boundary for every child ticket and adds the
approved reproducible acceptance lane: named fresh-launch commands plus
explicit `--connect` commands for a pre-existing loopback MCP session. It also
locks the earlier product decisions around registered-pass composition,
existing function-override persistence, and the newest-first SQLite explorer
and transactional cleaner.
