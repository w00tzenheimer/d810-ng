# Rhad Fourth GENERATED Operation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Prove reference-order 16 `setcc_indexed_table` at `0x40A77C` as the fourth distinct reference operation shape through the accepted shared GENERATED architecture.

**Architecture:** A new immutable compiler input and dedicated FragmentPlan normalization subtype persist the exact flag, index, table-entry, decode, and target derivation. Rows 14-16 join the accepted four operations in one dependency-closed FRONTEND_NORMALIZATION batch; the current detached PREOPT preparation and graph-free GENERATED coordinator publish the prepared one-block conditional with no new producer, gateway, or transaction representation.

**Tech Stack:** Python 3.13, pytest, IDA/Hex-Rays 9.3 runtime, SQLite diagnostics, ast-grep, import-linter, graphify, tk.

## Global Constraints

- Work only in `/Users/mahmoud/src/idapro/d810/.worktrees/lrea-portable-cfg-integration`.
- Preserve `tests/system/e2e/test_rhad_generated_microcode_capture.py` unchanged.
- Preserve the accepted `fd81625f8` 33/33 receipt, 24 witnesses, and permanent `0x40A605`, `0x40A619`, and `0x40A6A4` checksums.
- Keep FRONTEND_NORMALIZATION and the accepted producer, PatchPlan, immutable preflight, final binding, coordinator, gateway, receipt, poison, restart, and maturity authority.
- No live-MBA semantic discovery, sample-EA production guard, second producer, second gateway, new transaction representation, direct DeferredGraphModifier path, `mba.build_graph()` at GENERATED, use-def construction, local optimization, partial sibling set, CALLS planning/mutation/redo, or poisoned-generation continuation.
- Unsupported or ambiguous shapes reject before live mutation with a typed first failed obligation.
- Every production change follows a focused RED-GREEN cycle and a logical commit.

---

### Task 1: Immutable row-16 evidence and design record

**Files:**
- Create: `docs/experiments/rhad-a560-setcc-indexed-table-row16-evidence.json`
- Create: `docs/superpowers/specs/2026-07-26-rhad-fourth-generated-operation-design.md`
- Create: `docs/superpowers/plans/2026-07-26-rhad-fourth-generated-operation.md`
- Modify through tk: `.tickets/lrea-t5gy.md`

**Produces:** A pinned, independently derived selection and the first unmet typed obligation.

- [ ] Recheck inventory order 16 and dependency rows 14-15 against the 228-row ledger.
- [ ] Recheck pinned reference commit, exact native bytes, table entries, ESI key, decoded targets, and immutable input SHA-256.
- [ ] Validate the JSON with `python -m json.tool`, run `git diff --check`, and commit evidence only.

### Task 2: Typed FragmentPlan and compiler vocabulary

**Files:**
- Modify: `tests/unit/transforms/test_fragment_plan.py`
- Modify: `tests/unit/transforms/test_rhad_reference_compiler.py`
- Modify: `src/d810/transforms/fragment_plan.py`
- Modify: `src/d810/transforms/rhad_reference_compiler.py`

**Interfaces:**
- Produces: `FragmentSetccIndexedTableEntry`, `FragmentSetccIndexedTableEvidence`, `FragmentSetccIndexedTableNormalization`, and `RhadSetccIndexedTableRoute`.
- Consumes: existing `FragmentOperation`, `FragmentFlagCorridor`, `FragmentReferenceRouteAuthority`, `RouteOracleRun`, and native-body/work-item ownership.

- [ ] Write a FragmentPlan test whose hand-derived literal evidence catches a wrong index, stride, entry address/value, additive decode, or target orientation.
- [ ] Run the exact test and confirm RED because the typed setcc-table classes do not exist.
- [ ] Implement only the immutable portable types and validation; run the test GREEN.
- [ ] Write compiler positive and rejection tests for missing body proof, unselected scope, foreign oracle, anchor ambiguity, flag/setcc ordering, extension, stride, table identity, raw entries, decoded targets, closure, and dependency order.
- [ ] Run the compiler tests and confirm RED because `RhadSetccIndexedTableRoute` is not admitted.
- [ ] Compile the route to one conditional operation, dedicated normalization, exact flag corridor, and reference authority; run FragmentPlan and compiler suites GREEN.
- [ ] Commit compiler vocabulary separately.

### Task 3: Seven-operation batch semantics and complete closure

**Files:**
- Modify: `tests/unit/hexrays/preanalysis/test_rhad_generated_checksum.py`
- Modify: `src/d810/manager/rhad_generated_checksum.py`

**Interfaces:**
- Consumes: `RhadSetccIndexedTableRoute` and the dedicated FragmentPlan normalization.
- Produces: the dependency chain `0x40A74A -> 0x40A764 -> 0x40A77C`, complete imported body/template roots, and derived batch exits.

- [ ] Write a focused test asserting all seven operation IDs in reference order, exact row-16 table payload, dependency chain, imported anchors, source/target roots, selected obligations, proof IDs, and seven derived exits.
- [ ] Run it and confirm RED on the absent rows 14-16 batch evidence.
- [ ] Extend only typed batch data and base-plan derivation; retain exact-input `NativePreanalysisKey` dispatch.
- [ ] Add pre-live rejection coverage for incomplete target closure or table evidence.
- [ ] Run producer/compiler suites GREEN and commit operation semantics separately.

### Task 4: Graph-free prepared setcc conditional realization

**Files:**
- Modify: `tests/system/runtime/hexrays/test_detached_snippet_import.py`
- Modify: `tests/system/runtime/test_semantic_fragment_backend.py`
- Modify only if the RED proves necessary: `src/d810/hexrays/mutation/detached_handler_island.py`
- Modify: `src/d810/hexrays/mutation/semantic_fragment_backend.py`

**Interfaces:**
- Consumes: one prepared imported block whose tail is the typed signed predicate and whose exact instruction origins are recorded.
- Produces: one graph-free conditional binding with taken target blockref and physical false-target fallthrough through the existing coordinator.

- [ ] Write a runtime test that reaches the current `GENERATED profile admits only the proven conditional-select route` rejection for a valid setcc-table normalization.
- [ ] Run it and confirm RED for that precise missing contract.
- [ ] Add the narrow setcc realization branch; prove native-body proof, reference authority, exact source tail, target bindings, false-target adjacency, and no helper before any write.
- [ ] Add zero-write preflight rejection tests for wrong orientation, origin, target, or adjacency and poison/no-continue coverage for post-write failure.
- [ ] Run detached-import, backend, gateway, and GENERATED transaction suites GREEN; commit backend support separately.

### Task 5: SQLite lifecycle diagnostics

**Files:**
- Modify: `tests/system/e2e/test_rhad_generated_checksum_publication.py`
- Modify only as RED requires: `src/d810/manager/rhad_generated_checksum.py`

**Interfaces:**
- Consumes: typed seven-operation plan and committed receipt.
- Produces: persisted row-16 evidence/plan identity and operation-derived maturity observations through CMAT_FINAL.

- [ ] Add exact DB assertions for all seven reference payloads, row-16 evidence identity, plan identity, selected scope, preflight/final bindings, one receipt, and every creation witness.
- [ ] Run the E2E and confirm RED before diagnostics change.
- [ ] Generalize maturity observation from typed operation evidence so the new one-block conditional is observed without sample-EA branching; keep CALLS observation-only.
- [ ] Prove selected transfer absence, target survival while source exists, legitimate retirement, no residual ijmp/icall, no poison/rollback/redo/INTERR, and CMAT_FINAL.
- [ ] Commit diagnostic support separately.

### Task 6: Permanent regression and full acceptance

**Files:**
- Modify: `tests/system/e2e/test_rhad_generated_checksum_publication.py`
- Modify: `docs/experiments/rhad-a560-indirect-jump-coverage-summary.json`
- Modify through tk: `.tickets/lrea-t5gy.md`

- [ ] Preserve the accepted `0x40A605`, `0x40A619`, and `0x40A6A4` assertions and add the exact `0x40A77C` semantic checksum.
- [ ] Run focused compiler, FragmentPlan, producer, and GENERATED-publication suites.
- [ ] Run transaction/runtime suites and compare inherited full-backend failures without chasing the two known dispatcher-pattern DSL failures.
- [ ] Run the exact A560 E2E and retain the authoritative SQLite DB path, receipt count, witness count, and highest maturity.
- [ ] Run the protected Hodur/Sub7FFD/Tigress/Approov matrix and strict 41-row parity gate.
- [ ] Run `sg scan --config sgconfig.yml --report-style short` and `PYTHONPATH=src lint-imports --config .importlinter` from the target worktree.
- [ ] Recheck the input SHA-256, run `graphify update .`, inspect final diff/status, and confirm the user-owned capture test is unchanged.
- [ ] Commit regression coverage, proof record, and ticket note as separate logical commits; keep `lrea-t5gy` in progress and stop before any fifth shape or constant materialization.
- [ ] Run `simba codex-finalize` and mark the bounded goal complete only after a requirement-by-requirement audit.
