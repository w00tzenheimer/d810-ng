# Rhad Third GENERATED Operation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Prove reference-order 8 `existing_conditional_plus_indirect` at `0x40A6A4` as the third distinct operation variant through the accepted GENERATED architecture.

**Architecture:** A new immutable imported-conditional reference type compiles to the existing FragmentPlan conditional-normalization vocabulary. The current PREOPT template materializer prepares the source/select/join body; a narrow typed GENERATED staging contract binds its semantic arms through the existing coordinator in the same dependency-closed transaction as the two accepted operations and direct dependency `0x40A68A`.

**Tech Stack:** Python 3.13, pytest, IDA/Hex-Rays 9.3 runtime, SQLite diagnostics, ast-grep, import-linter, graphify, tk.

## Global Constraints

- Work only in `/Users/mahmoud/src/idapro/d810/.worktrees/lrea-portable-cfg-integration`.
- Preserve `tests/system/e2e/test_rhad_generated_microcode_capture.py` unchanged.
- Keep `FRONTEND_NORMALIZATION` and the existing producer, PatchPlan, coordinator, gateway, receipt, poison/restart, and maturity authority.
- No live-MBA compiler dependency, block serial evidence, sample-EA dispatch guard, direct DeferredGraphModifier path, `mba.build_graph()` at GENERATED, use-def construction, local optimization, partial sibling batch, CALLS planning/mutation, or continued poisoned generation.
- Every production change follows a focused RED-GREEN cycle and logical commit.

---

### Task 1: Coverage and selected evidence

**Files:**
- Create: `docs/experiments/rhad-a560-indirect-jump-coverage-summary.json`
- Create: `docs/superpowers/specs/2026-07-26-rhad-third-generated-operation-design.md`
- Create: `docs/superpowers/plans/2026-07-26-rhad-third-generated-operation.md`
- Modify through tk: `.tickets/lrea-t5gy.md`

**Produces:** An auditable preimplementation selection of row 8 and direct dependency row 7.

- [ ] Validate all variant totals and current accepted receipt IDs against the 228-row inventory.
- [ ] Validate native predicate orientation with the immutable input disassembly.
- [ ] Record the missing `0x40A6A6` arm and imported conditional realization contract in `lrea-t5gy`.
- [ ] Validate JSON, `git diff --check`, and commit evidence only.

### Task 2: Portable imported-conditional compiler vocabulary

**Files:**
- Modify: `tests/unit/transforms/test_rhad_reference_compiler.py`
- Modify: `src/d810/transforms/rhad_reference_compiler.py`

**Produces:** `RhadExistingConditionalRoute` and compilation to `FragmentComputedBranchNormalization` with `FragmentImportedConditionalSelectEnvelope`.

- [ ] Write a focused positive test using source `0x40A68C`, selected value `0x40A69A`, join `0x40A6A0`, transfer `0x40A6A4`, and exact two arms.
- [ ] Run it and confirm RED because the typed route class is absent.
- [ ] Add independent rejection tests for missing reference authority, incomplete arms, wrong predicate orientation, absent native-body proof, mismatched `RouteOracleRun`, unselected frontend scope, and ambiguous/out-of-corridor anchors.
- [ ] Implement the required immutable type and compiler rule without IDA imports.
- [ ] Run the full compiler and FragmentPlan suites and commit compiler vocabulary alone.

### Task 3: Complete typed batch evidence and dependency closure

**Files:**
- Modify: `tests/unit/hexrays/preanalysis/test_rhad_generated_checksum.py`
- Modify: `src/d810/manager/rhad_generated_checksum.py`

**Produces:** Four ordered operations, complete `0x40A6A6` and `0x40A800` closures, template roots, imported bodies, proof IDs, and selected obligations.

- [ ] Write a focused test asserting the accepted two operations, `0x40A68A` dependency, selected `0x40A6A4`, exact dependencies, roots, ranges, closures, and final exits.
- [ ] Run it and confirm RED on the absent third route/batch evidence.
- [ ] Extend only typed `RhadGeneratedReferenceBatch` data and derived base-plan construction.
- [ ] Prove unsupported/incomplete branch evidence rejects before producer publication.
- [ ] Run producer/compiler suites and commit batch evidence separately.

### Task 4: Graph-free imported conditional realization

**Files:**
- Modify: `tests/system/runtime/hexrays/test_detached_snippet_import.py`
- Modify: `tests/system/runtime/test_semantic_fragment_backend.py`
- Modify only as RED requires: `src/d810/hexrays/mutation/detached_handler_island.py`
- Modify only as RED requires: `src/d810/hexrays/mutation/semantic_fragment_backend.py`

**Produces:** Typed binding of a detached imported conditional source/select/join to exact semantic arms during the existing GENERATED transaction.

- [ ] Write a runtime test whose prepared imported conditional operation is rejected by the current GENERATED realizer.
- [ ] Run it and confirm RED on `GENERATED profile admits only the proven conditional-select route` or the earlier typed missing obligation.
- [ ] Add the smallest staging interface that proves prepared role identities, exact instruction origins, body proof, and target bindings before writing.
- [ ] Bind the imported conditional through existing gateway operations with no helper, graph construction, independent receipt, or partial realization.
- [ ] Add poison/no-continue and immutable-preflight rejection coverage.
- [ ] Run detached-import, backend, gateway, and GENERATED transaction suites; commit realization independently.

### Task 5: SQLite and exact semantic canary

**Files:**
- Modify: `tests/system/e2e/test_rhad_generated_checksum_publication.py`
- Modify only as RED requires: `src/d810/manager/rhad_generated_checksum.py`

**Produces:** One diagnostic DB proving the complete four-operation batch through CMAT_FINAL.

- [ ] Add assertions for all reference payloads, inventory identity, selected obligations, plan serialization, phases, live bindings, receipt, and every creation witness.
- [ ] Add exact GENERATED/PREOPT/LOCOPT/CALLS canaries for `0x40A605`, `0x40A619`, `0x40A68A`, and `0x40A6A4`.
- [ ] Confirm RED before diagnostic/observer changes.
- [ ] Generalize maturity observation from typed operation evidence; keep CALLS observation-only.
- [ ] Prove one complete receipt, all witnesses reconstructible, no residual selected indirect transfer, no poison/rollback/redo/INTERR, and CMAT_FINAL.
- [ ] Commit diagnostics and regression repair separately.

### Task 6: Full acceptance and ticket closeout note

**Files:**
- Modify through tk: `.tickets/lrea-t5gy.md`
- Update: `docs/experiments/rhad-a560-indirect-jump-coverage-summary.json`

- [ ] Run focused compiler and GENERATED-publication suites.
- [ ] Run transaction/runtime suites and compare the full semantic-backend file to its inherited four-failure baseline.
- [ ] Run exact bounded A560 E2E and preserve its diagnostic DB path.
- [ ] Run the 13-node Hodur/Sub7FFD/Tigress/Approov matrix and verify known dispatcher failures are not broadened or hidden.
- [ ] Run strict 41-row parity, ast-grep, all 14 import contracts, and immutable input SHA check.
- [ ] Run `graphify update .`, inspect the final diff/status, and preserve the user-owned capture test.
- [ ] Amend `lrea-t5gy` with final SHA, DB, receipt/witness counts, highest maturity, state flags, gates, and next unproved variant. Do not close it.
- [ ] Run `simba codex-finalize`, commit final coverage/ticket evidence, and mark the bounded goal complete only after requirement-by-requirement audit.
