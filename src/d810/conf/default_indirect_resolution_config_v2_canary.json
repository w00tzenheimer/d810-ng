{
  "description": "Config-v2 explicit opt-in canary for default_indirect_resolution.json. The existing project configuration path remains the default runtime; this project is selectable only when explicit config-v2 mode is desired.",
  "ins_rules": [],
  "blk_rules": [],
  "additional_configuration": {
    "pipeline_v2_mode": "config-v2",
    "config_v2_canary": {
      "source_config": "default_indirect_resolution.json",
      "source_shadow": "default_indirect_resolution.pipeline_v2.json",
      "representative_row": "default_indirect_resolution_branch_call_config_v2_canary_branch",
      "runtime_source": "pipeline_v2"
    },
    "pipeline_v2": [
      {
        "pass": "indirect-branch-resolver",
        "scope": "block",
        "maturity": {
          "range": {
            "min": "ir.canonical",
            "max": "ir.global.optimized"
          }
        },
        "requires": {
          "capabilities": [
            "legacy_flow_rule_adapter"
          ]
        },
        "migration": {
          "source_config": "default_indirect_resolution.json",
          "source_section": "blk_rules",
          "source_rule": "IndirectBranchResolver"
        },
        "options": {
          "legacy_rule": "IndirectBranchResolver"
        }
      },
      {
        "pass": "indirect-call-resolver",
        "scope": "block",
        "maturity": {
          "range": {
            "min": "ir.canonical",
            "max": "ir.global.optimized"
          }
        },
        "requires": {
          "capabilities": [
            "legacy_flow_rule_adapter"
          ]
        },
        "migration": {
          "source_config": "default_indirect_resolution.json",
          "source_section": "blk_rules",
          "source_rule": "IndirectCallResolver"
        },
        "options": {
          "legacy_rule": "IndirectCallResolver"
        }
      }
    ]
  }
}
